import {useEffect,useRef} from 'react'; export default function Reveal({children,className=''}){const r=useRef(null);useEffect(()=>{const el=r.current;if(!el)return;const o=new IntersectionObserver(([e])=>{if(e.isIntersecting){el.classList.add('is-visible');o.disconnect()}},{threshold:.08});o.observe(el);return()=>o.disconnect()},[]);return <div ref={r} className={`reveal ${className}`}>{children}</div>}
