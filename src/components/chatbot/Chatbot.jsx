import {useState} from 'react';import {Bot,Send,X} from 'lucide-react';export default function Chatbot(){const [open,setOpen]=useState(false);const [text,setText]=useState('');const [msgs,setMsgs]=useState([{from:'bot',text:'Hi! I can help you understand services, process, pricing, or how to start.'}]);function send(){if(!text.trim())return;const q=text.trim();setMsgs(m=>[...m,{from:'user',text:q},{from:'bot',text:'Thanks! Share your project goals and I can point you toward the best next step.'}]);setText('')}return <><button className="chat-toggle" onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Bot/>}</button>{open&&<div className="chatbot"><div className="chat-head"><div><b>Studio Assistant</b><small>Usually replies instantly</small></div><Bot/></div><div className="chat-body">{msgs.map((m,i)=><div key={i} className={'msg '+m.from}>{m.text}</div>)}</div><div className="chat-input"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Ask something…"/><button onClick={send}><Send size={16}/></button></div></div>}</>}
