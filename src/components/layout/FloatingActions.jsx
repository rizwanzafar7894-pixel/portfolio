import {ArrowUp,MessageCircle} from 'lucide-react';import {useEffect,useState} from 'react'; export default function FloatingActions(){const [show,setShow]=useState(false);useEffect(()=>{const f=()=>setShow(scrollY>500);addEventListener('scroll',f);f();return()=>removeEventListener('scroll',f)},[]);return <div className="floating"><a className="wa" href="https://wa.me/" aria-label="WhatsApp"><MessageCircle/></a>{show&&<button onClick={()=>scrollTo({top:0,behavior:'smooth'})} aria-label="Back to top"><ArrowUp/></button>}</div>}
