import {Menu,X,Sun,Moon,ArrowUpRight} from 'lucide-react'; import {useState} from 'react'; import useTheme from '../../hooks/useTheme';
export default function Navbar(){const [open,setOpen]=useState(false);const [dark,toggle]=useTheme();const links=['About','Services','Work','Pricing','FAQ','Contact'];return <header className="nav"><a href="#top" className="brand">R<span>.</span></a><nav className={open?'open':''}>{links.map(x=><a key={x} href={'#'+x.toLowerCase()} onClick={()=>setOpen(false)}>{x}</a>)}<a className="nav-cta" href="#contact">Let’s talk <ArrowUpRight size={15}/></a></nav><div className="nav-actions"><button className="icon-btn" onClick={toggle} aria-label="Toggle theme">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><button className="icon-btn menu-btn" onClick={()=>setOpen(v=>!v)} aria-label="Menu">{open?<X/>:<Menu/>}</button></div></header>}
