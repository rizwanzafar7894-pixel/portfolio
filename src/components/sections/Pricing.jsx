import {useState} from 'react';import Reveal from '../common/Reveal';import {pricing} from '../../data/defaults';export default function Pricing(){const [yearly,setYearly]=useState(false);return <section id="pricing" className="section pricing"><Reveal><div className="section-kicker">05 — Pricing</div><div className="section-head"><h2>Simple packages.<br/><span>Serious outcomes.</span></h2><div className="billing"><button className={!yearly?'active':''} onClick={()=>setYearly(false)}>Monthly</button><button className={yearly?'active':''} onClick={()=>setYearly(true)}>Yearly <i>Save 15%</i></button></div></div><div className="price-grid">{pricing.map(([n,p,d],i)=><article className={'price-card '+(i===1?'popular':'')} key={n}>{i===1&&<label>Most popular</label>}<h3>{n}</h3><div className="price">{yearly?(p.replace(/\d+/,m=>Math.round(+m*0.85))):p}<small>/ project</small></div><p>{d}</p><a href="#contact" className="btn secondary">Choose {n}</a><ul><li>Discovery & strategy</li><li>Responsive design</li><li>Development handoff</li><li>Structured revisions</li></ul></article>)}</div></Reveal></section>}
