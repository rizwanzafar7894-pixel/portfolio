import {useState} from 'react';import {ArrowUpRight,CheckCircle} from 'lucide-react';import Reveal from '../common/Reveal';import {submitDocument} from '../../services/firestore';export default function Contact(){const [form,setForm]=useState({name:'',email:'',message:''});const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);try{await submitDocument('contactMessages',{...form,status:'new'});setSent(true);setForm({name:'',email:'',message:''})}catch{alert('Please configure Firebase before submitting.')}finally{setBusy(false)}}return <section id="contact" className="section contact"><Reveal><div className="contact-grid"><div><div className="section-kicker">08 — Contact</div><h2>Have an idea?<br/><span>Let’s make it real.</span></h2><p className="lead">Tell me what you’re building, where you’re stuck, or what you want to improve. I’ll get back with practical next steps.</p><div className="contact-note"><b>Typical reply</b><span>Within 1 business day</span></div></div><form className="contact-form-card" onSubmit={submit}>{sent?<div className="success"><CheckCircle/><h3>Message received.</h3><p>Thanks — I’ll be in touch soon.</p><button type="button" className="btn secondary" onClick={()=>setSent(false)}>Send another</button></div>:<><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Project details<textarea required rows="5" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Tell me a little about your project..."/></label><button disabled={busy} className="btn primary" type="submit">{busy?'Sending…':'Send inquiry'} <ArrowUpRight size={16}/></button></>}</form></div></Reveal></section>}
