import {useEffect,useState} from 'react';import {ArrowUpRight,CheckCircle} from 'lucide-react';import Reveal from '../common/Reveal';import {submitDocument,getDocument} from '../../services/firestore';export default function Contact(){const [settings,setSettings]=useState({}),[form,setForm]=useState({name:'',email:'',message:''}),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{getDocument('siteSettings','public').then(x=>x&&setSettings(x)).catch(()=>{})},[]);async function submit(e){e.preventDefault();setBusy(true);setError('');try{await submitDocument('contactMessages',{...form,status:'new',source:'portfolio-contact'});setSent(true);setForm({name:'',email:'',message:''})}catch(e){setError(e.message||'Unable to send your message. Please try again.')}finally{setBusy(false)}}return <section id="contact" className="section contact"><Reveal><div className="contact-grid"><div><div className="section-kicker">08 — Contact</div><h2>Have an idea?<br/><span>Let’s make it real.</span></h2><p className="lead">Tell me what you’re building, where you’re stuck, or what you want to improve. I’ll get back with practical next steps.</p><div className="contact-note"><b>Typical reply</b><span>{settings.contactReply||'Within 1 business day'}</span></div></div><form className="contact-form-card" onSubmit={submit}>{sent?<div className="success"><CheckCircle/><h3>Message received.</h3><p>Thanks — I’ll be in touch soon.</p><button type="button" className="btn secondary" onClick={()=>setSent(false)}>Send another</button></div>:<><label>Name<input required maxLength="100" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></label><label>Email<input required maxLength="160" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Project details<textarea required maxLength="5000" rows="5" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Tell me a little about your project..."/></label>{error&&<p role="alert" className="admin-error">{error}</p>}<button disabled={busy} className="btn primary" type="submit">{busy?'Sending…':'Send inquiry'} <ArrowUpRight size={16}/></button></>}</form></div></Reveal></section>}
