import {useEffect,useState} from 'react';import Reveal from '../common/Reveal';import {getDocument} from '../../services/firestore';
export default function About(){const [s,setS]=useState({});useEffect(()=>{getDocument('siteSettings','public').then(x=>x&&setS(x)).catch(()=>{})},[]);return <section id="about" className="section about"><Reveal><div className="section-kicker">01 — About</div><div className="split"><h2>Good design is<br/><span>clarity with character.</span></h2><div><p className="lead">{s.aboutLead||'I combine product thinking, visual design and front-end craft to turn ambitious ideas into digital experiences that feel simple, useful and distinctly yours.'}</p><p>{s.aboutBody||'I care about the details users notice and the ones they never should. Every decision has a purpose — from information architecture to motion, performance and accessibility.'}</p><a href="#contact" className="text-link">Work together →</a></div></div></Reveal></section>}
