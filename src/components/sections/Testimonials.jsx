import {useState} from 'react';import Reveal from '../common/Reveal';import {testimonials} from '../../data/defaults';export default function Testimonials(){const [i,setI]=useState(0);const t=testimonials[i];return <section className="section testimonials"><Reveal><div className="section-kicker">06 — Kind words</div><div className="testimonial"><div className="quote">“</div><blockquote>{t[2]}</blockquote><div className="person"><span>{t[0][0]}</span><div><b>{t[0]}</b><small>{t[1]}</small></div></div><div className="slider-controls"><button onClick={()=>setI((i-1+testimonials.length)%testimonials.length)}>←</button><span>{String(i+1).padStart(2,'0')} / {String(testimonials.length).padStart(2,'0')}</span><button onClick={()=>setI((i+1)%testimonials.length)}>→</button></div></div></Reveal></section>}
