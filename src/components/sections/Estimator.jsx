import {useState} from 'react';import Reveal from '../common/Reveal';export default function Estimator(){const [s,setS]=useState([0,0,0]);const groups=[['What do you need?',['Landing page','Web app','Mobile app','Brand identity']],['Project size',['Focused','Standard','Complex','Large scale']],['When do you need it?',['Flexible','1–2 months','3–4 weeks','ASAP']]];const total=499+s[0]*350+s[1]*450+s[2]*300;return <section className="section estimator"><Reveal><div className="estimator-card"><div><div className="section-kicker">Quick estimate</div><h2>Let’s put a<br/><span>number on it.</span></h2><p>Choose a few options and get a planning estimate. Your final scope is always confirmed together.</p><strong>From ${total.toLocaleString()}</strong></div><div className="est-options">{groups.map(([g,opts],gi)=><div key={g}><small>{g}</small><div>{opts.map((o,oi)=><button className={s[gi]===oi?'selected':''} onClick={()=>setS(a=>a.map((v,k)=>k===gi?oi:v))} key={o}>{o}</button>)}</div></div>)}<a href="#contact" className="btn primary">Get a tailored quote →</a></div></div></Reveal></section>}
