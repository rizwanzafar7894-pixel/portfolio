import {ArrowDown,ArrowUpRight,Sparkles} from 'lucide-react';import {useEffect,useState} from 'react';import {getDocument} from '../../services/firestore';
const FALLBACK_PROFILE='https://images.unsplash.com/photo-1560250097-0b93528c311a?auto=format&fit=crop&w=900&q=90';
export default function Hero(){const words=['digital products','mobile apps','brand identities','user experiences'];const [i,setI]=useState(0);const [profile,setProfile]=useState(FALLBACK_PROFILE);const [settings,setSettings]=useState({});useEffect(()=>{const t=setInterval(()=>setI(v=>(v+1)%words.length),2400);return()=>clearInterval(t)},[]);useEffect(()=>{getDocument('siteSettings','public').then(data=>{if(data){setSettings(data);if(data.profileImageUrl)setProfile(data.profileImageUrl)}}).catch(()=>{})},[]);return <section id="top" className="hero section"><div className="hero-orb orb-a"/><div className="hero-orb orb-b"/><div className="hero-layout"><div className="hero-copy-column"><div className="eyebrow"><Sparkles size={14}/> Independent designer & developer</div><h1>I build <em>{words[i]}</em><br/>that move businesses forward.</h1><p className="hero-copy">Strategy, design and development in one focused workflow — from the first idea to a polished product people love to use.</p><div className="hero-actions"><a className="btn primary" href="#work">Explore my work <ArrowUpRight size={17}/></a><a className="text-link" href="#contact">Start a project <ArrowDown size={16}/></a></div><div className="hero-meta"><span><b>7+</b> years experience</span><span><b>60+</b> projects shipped</span><span><b>14</b> countries reached</span></div></div><div className="hero-profile"><div className="profile-frame"><img src={profile} alt={settings.profileName||'Profile'} onError={e=>{e.currentTarget.src=FALLBACK_PROFILE}}/><span className="profile-status" aria-label="Available for work"/></div><strong>{settings.profileName||'Muhammad Rizwan'}</strong><small>{settings.profileRole||'UI/UX Designer & Developer'}</small></div></div></section>}
