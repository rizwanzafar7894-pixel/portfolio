import {useState} from 'react';import {Plus} from 'lucide-react';import Reveal from '../common/Reveal';import {faqs} from '../../data/defaults';export default function FAQ(){const [open,setOpen]=useState(0);return <section id="faq" className="section faq"><Reveal><div className="section-kicker">07 — FAQ</div><div className="split"><h2>Questions,<br/><span>answered.</span></h2><div className="faq-list">{faqs.map(([q,a],i)=><div className={'faq-item '+(open===i?'open':'')} key={q}><button onClick={()=>setOpen(open===i?-1:i)}><span>{q}</span><Plus/></button>{open===i&&<p>{a}</p>}</div>)}</div></div></Reveal></section>}
