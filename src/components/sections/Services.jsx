import Reveal from '../common/Reveal';import {ArrowUpRight} from 'lucide-react';import {services} from '../../data/defaults';export default function Services(){return <section id="services" className="section services"><Reveal><div className="section-kicker">02 — Services</div><div className="section-head"><h2>One partner.<br/><span>Full digital craft.</span></h2><p>From a blank canvas to a production-ready experience, I keep the process connected.</p></div><div className="service-grid">{services.map(([n,t,d])=><article className="service-card" key={n}><span>{n}</span><h3>{t}</h3><p>{d}</p><ArrowUpRight/></article>)}</div></Reveal></section>}
