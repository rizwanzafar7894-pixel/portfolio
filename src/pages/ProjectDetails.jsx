import {useEffect,useState} from 'react';import {ArrowLeft,ArrowUpRight,Github,ExternalLink} from 'lucide-react';import {useParams,Link} from 'react-router-dom';import {projects as fallback} from '../data/defaults';import {getPublishedBySlug} from '../services/firestore';
export default function ProjectDetails(){const {slug}=useParams();const [p,setP]=useState(fallback.find(x=>x.id===slug)||null);const [loading,setLoading]=useState(!p);useEffect(()=>{getPublishedBySlug('projects',slug).then(x=>{if(x)setP(x)}).catch(()=>{}).finally(()=>setLoading(false))},[slug]);useEffect(()=>{if(p){document.title=`${p.title} — Portfolio`;window.scrollTo({top:0,behavior:'smooth'})}},[p]);if(loading)return <main className="notfound"><h1>Loading project…</h1></main>;if(!p)return <div className="notfound"><div><h1>Project not found</h1><Link to="/">Back home</Link></div></div>;const image=p.imageUrl||p.img;const gallery=p.gallery||[];const tags=p.tags||[];const tech=p.technologies||p.techStack||[];return <main className="detail project-detail-full"><div className="post-topbar"><Link className="text-link" to="/#work"><ArrowLeft size={16}/> Back to projects</Link><div className="project-actions">{p.githubUrl&&<a className="icon-btn" href={p.githubUrl} target="_blank" rel="noreferrer" aria-label="GitHub"><Github size={17}/></a>}{p.liveUrl&&<a className="icon-btn" href={p.liveUrl} target="_blank" rel="noreferrer" aria-label="Live project"><ExternalLink size={17}/></a>}</div></div>{image&&<img className="post-hero-image" src={image} alt={p.title}/>}<header className="post-header project-header"><div className="post-meta"><span>{p.category||p.cat||'Project'}</span>{p.client&&<span>Client: {p.client}</span>}{p.year&&<span>{p.year}</span>}{p.duration&&<span>{p.duration}</span>}</div><h1>{p.title}</h1><p className="post-lead">{p.description||p.desc||p.shortDescription}</p></header><div className="project-case-study"><div className="chips">{tags.map(x=><span key={x}>{x}</span>)}{tech.map(x=><span key={x}>{x}</span>)}</div>{p.result&&<section><small className="section-kicker">Outcome</small><h2>{p.result}</h2></section>}{p.shortDescription&&p.description&&<section><small className="section-kicker">Overview</small><p>{p.description}</p></section>}{p.caseStudy&&<section className="post-content" dangerouslySetInnerHTML={{__html:p.caseStudy}}/>}{gallery.length>0&&<section><small className="section-kicker">Project gallery</small><div className="detail-gallery">{gallery.map((url,i)=><img key={url+i} src={url} alt={`${p.title} ${i+1}`} loading="lazy"/>)}</div></section>}{(p.liveUrl||p.githubUrl)&&<section className="project-cta"><h2>Explore the work</h2><div className="hero-actions">{p.liveUrl&&<a className="btn primary" href={p.liveUrl} target="_blank" rel="noreferrer">Live project <ArrowUpRight size={16}/></a>}{p.githubUrl&&<a className="btn secondary" href={p.githubUrl} target="_blank" rel="noreferrer">View source <Github size={16}/></a>}</div></section>}</div></main>}
