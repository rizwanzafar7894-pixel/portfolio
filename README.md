# Premium Portfolio — React

Vite + React portfolio foundation converted into reusable components with Firebase-ready forms and CMS-ready service boundaries.

## Run
`npm install`
`npm run dev`

## Production
`npm run build`

Configure Firebase values from `.env.example`. Public pages never contain Backblaze secrets; privileged uploads should go through a server/API endpoint.
